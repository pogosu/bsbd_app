# 🚔 Система управления штрафами ГИБДД

Решение для автоматизации учета нарушений ПДД, управления платежами и разграничения прав доступа.

---

## 🚀 Быстрый запуск

### 1️⃣ База данных (PostgreSQL)
1. Создайте базу данных:
   ```sql
   CREATE DATABASE gibdd_fines;
   ```
2. Выполните скрипт инициализации:
   - Файл: `init_database.sql`
   - *Этот скрипт автоматически создаст все таблицы, роли, пользователей и настроит политики безопасности (RLS).*

### 2️⃣ Подготовка окружения
Убедитесь, что у вас установлен **Python 3.x**. Установите необходимые зависимости:
```bash
pip install psycopg2
```

### 3️⃣ Запуск приложения
Запустите графический интерфейс:
```bash
python app.py
```

---

## 👥 Учетные записи для тестирования

| Роль | Логин | Пароль | Описание |
| :--- | :--- | :--- | :--- |
| **Администратор** | `AdminUser` | `admin123` | Полный доступ ко всем данным |
| **Инспектор** | `Inspector1` | `inspector1` | Создание штрафов, просмотр всех данных |
| **Гражданин** | `Citizen1` | `citizen1` | Просмотр своих штрафов, оплата |

---

## 🛠 Технологический стек
- **Backend**: PostgreSQL (Triggers, Functions, RLS)
- **Frontend**: Python (Tkinter)
- **Database Driver**: Psycopg2

---

> [!NOTE]
> При входе под пользователем "Гражданин", система автоматически фильтрует данные, показывая только те записи, которые относятся к текущему пользователю.
